# Dev server / docker compose standalone (Next.js)
# PORT=3000

# Dipakai server Next untuk meneruskan /api/v1 ke Go.
# Browser memakai origin yang sama (/api/v1), bukan URL ini.
GIS_API_URL=http://127.0.0.1:8080
